services:
  app:
    build:
      context: .
      args:
        VITE_API_BASE_URL: /api
        VITE_WS_BASE_URL: /ws
    env_file:
      - ./backend/.env
    ports:
      - "8080:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
